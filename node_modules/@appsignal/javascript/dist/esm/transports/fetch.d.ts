import { ITransport } from "../interfaces/ITransport";
export declare class FetchTransport implements ITransport {
    url: string;
    constructor(url: string, headers?: {
        [key: string]: string;
    });
    send(data: string): Promise<any>;
}
//# sourceMappingURL=fetch.d.ts.map