import { ITransport } from "../interfaces/ITransport";
export declare class XHRTransport implements ITransport {
    url: string;
    constructor(url: string);
    send(data: string): Promise<any>;
}
//# sourceMappingURL=xhr.d.ts.map