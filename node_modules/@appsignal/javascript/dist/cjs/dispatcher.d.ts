import { Queue } from "./queue";
import { PushApi } from "./api";
declare type DispatcherOptions = {
    limit: number;
    initialDuration: number;
};
export declare class Dispatcher {
    options: DispatcherOptions;
    private _queue;
    private _api;
    private _retries;
    private _timerID;
    private _duration;
    constructor(queue: Queue, api: PushApi, options?: Partial<DispatcherOptions>);
    schedule(time?: number): number;
    reset(): void;
}
export {};
//# sourceMappingURL=dispatcher.d.ts.map